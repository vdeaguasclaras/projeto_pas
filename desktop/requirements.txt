# `opencv-python-headless` traz o mesmo módulo `cv2` sem a parte de janela do
# OpenCV — que este aplicativo não usa: quem desenha a interface é o Qt. Em
# Linux sem ambiente gráfico o pacote cheio nem importa (falta a libGL), e no
# executável ele só engorda o instalador. Se a máquina já tiver o
# `opencv-python` instalado, funciona igual; não vale desinstalar por isso.
opencv-python-headless>=4.10
numpy>=1.26
pypdfium2>=4.30
click>=8.1

# A janela. `PySide6-Essentials` em vez do `PySide6` cheio: o pacote completo
# traz WebEngine, 3D e multimídia, que esta janela não usa e que engordariam o
# .exe em algumas centenas de megabytes.
PySide6-Essentials>=6.7
